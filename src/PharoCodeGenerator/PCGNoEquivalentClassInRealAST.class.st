"
I am the error created when an attempt to send #classForRealAST is done to a PCGProgramNode class that has no equivalent AST node class in the real Pharo AST.
"
Class {
	#name : #PCGNoEquivalentClassInRealAST,
	#superclass : #PCGError,
	#category : #'PharoCodeGenerator-Errors'
}
